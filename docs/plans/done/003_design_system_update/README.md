# Design System — Baleinev Backoffice

Start with **AUDIT.md** (what's inconsistent today, with file:line evidence), then
**DESIGN-SYSTEM.md** (the tokens/component spec to fix it). `components/` is a ready-to-
drop React/Tailwind library — copy it into the app repo at `app/components/ui/`.

Built from a read of `2lazy2debug/baleinev-backoffice` (`main`) — see `github.md` at the
project root for sync details.
