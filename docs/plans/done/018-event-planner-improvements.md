The event planner application is already great. 

But the admin should be able to do the following things : 
- shift day duplication : oftentimes, the same shift-schema is reproduced across multiple days of an event. A duplicate option (small button, top right of a day, copy document icon) should be available that asks to which day (so available, non day off) it should be copied to. multiple days can be selected. it is a copy, not a reference. 

deploy directly the application with the latest available tag.