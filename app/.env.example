# PostgreSQL connection. Must match docker/.env (POSTGRES_USER, POSTGRES_PASSWORD,
# POSTGRES_DB, POSTGRES_PORT).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/baleinev_comptes?schema=public"

# Session signing key. Generate one with: openssl rand -base64 32
AUTH_SECRET=""

# Public URL of the app. Required by NextAuth outside development.
NEXTAUTH_URL="http://localhost:3000"

# First admin account, created or updated by `npm run db:seed`.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
ADMIN_NAME="Baleinev Admin"
