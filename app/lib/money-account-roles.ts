// Kept dependency-free (no prisma/next-auth imports) so the edge middleware
// can use it alongside lib/access.ts's server-side check.
export const MONEY_ACCOUNT_MANAGER_DEPARTMENT = "Comptabilité";
